[mysqld]
# Specifies the directory where MariaDB stores its data.
datadir = /var/lib/mysql
# This allows MariaDB to accept connections from any IP address, including external machines.
bind_address=0.0.0.0
# Port 3306 is the default port for MySQL and MariaDB.
port = 3306
